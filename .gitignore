target/
node_modules/
/package-lock.json
__pycache__/
dist/
data/
!website/data/
*.swp
*.swo
.DS_Store
.env
.env.*
!.env.example
.claude/
# Anchored to the repository root on purpose. A bare `CLAUDE.md` matches at any
# depth, which silently excluded the tracked upstream file `identity/CLAUDE.md`
# from this fork; anything deeper than the root is content, not agent scratch.
/CLAUDE.md
pnpm-lock.yaml
yarn.lock

.gstack/
.worktrees/
.vercel

.agentos-*.log
.agentos-dev.pids
.agentos-runtime/
.upstream-iii/
runtime/

iii: v1
scripts:
  install: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo build --release'
  start: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo run --release'
name: telemetry
runtime:
  kind: rust
description: OTel-style metric/span recording bridge — process memory, uptime, and metrics summary

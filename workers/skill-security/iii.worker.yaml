iii: v1
scripts:
  install: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo build --release'
  start: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo run --release'
name: skill-security
runtime:
  kind: rust
description: Skill capability gating — Ed25519 signature verification, content scanning, sandbox checks

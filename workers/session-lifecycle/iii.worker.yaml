iii: v1
scripts:
  install: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo build --release'
  start: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo run --release'
name: session-lifecycle
runtime:
  kind: rust
description: Session lifecycle state machine — transitions, reactions, and cron-driven health checks

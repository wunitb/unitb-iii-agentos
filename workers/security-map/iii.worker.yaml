iii: v1
scripts:
  install: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo build --release'
  start: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo run --release'
name: security-map
runtime:
  kind: rust
description: Mutual Authentication Protocol — challenge/respond/verify with HMAC-SHA256 over shared secrets

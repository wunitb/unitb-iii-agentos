iii: v1
scripts:
  install: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo build --release'
  start: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo run --release'
name: vault
runtime:
  kind: rust
description: Encrypted secret storage — AES-GCM-256 + scrypt KDF with auto-lock and access control

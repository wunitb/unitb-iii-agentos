iii: v1
scripts:
  install: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo build --release'
  start: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo run --release'
name: swarm
runtime:
  kind: rust
description: Multi-agent swarm orchestration — create, broadcast, collect, consensus, dissolve

iii: v1
scripts:
  install: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo build --release'
  start: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo run --release'
name: workflow
runtime:
  kind: rust
description: "Multi-step workflow execution — sequential, fanout, collect, conditional, loop"

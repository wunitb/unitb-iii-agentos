iii: v1
scripts:
  install: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo build --release'
  start: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo run --release'
name: channel-bluesky
runtime:
  kind: rust
description: "Bluesky/AT Protocol channel adapter — webhook receive + post via XRPC"

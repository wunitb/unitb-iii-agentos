iii: v1
scripts:
  install: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo build --release'
  start: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo run --release'
name: context-monitor
runtime:
  kind: rust
description: Context health scoring and proactive compression — health, compress, trim, prune, auto_optimize, stats

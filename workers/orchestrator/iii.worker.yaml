iii: v1
scripts:
  install: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo build --release'
  start: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo run --release'
name: orchestrator
runtime:
  kind: rust
description: Multi-step orchestrator — plan, execute, status, intervene, and shared workspace read/write

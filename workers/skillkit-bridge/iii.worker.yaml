iii: v1
scripts:
  install: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo build --release'
  start: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo run --release'
name: skillkit-bridge
runtime:
  kind: rust
description: SkillKit CLI bridge — search, install, list, recommend, scan

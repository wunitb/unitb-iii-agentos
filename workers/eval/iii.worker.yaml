iii: v1
scripts:
  install: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo build --release'
  start: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo run --release'
name: eval
runtime:
  kind: rust
description: Evaluation harness — invoke functions, score correctness/latency/cost/safety, suite aggregation

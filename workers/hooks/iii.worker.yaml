iii: v1
scripts:
  install: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo build --release'
  start: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo run --release'
name: hooks
runtime:
  kind: rust
description: Hook registration and firing — agent lifecycle, function-call interception, prompt mutation

iii: v1
scripts:
  install: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo build --release'
  start: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo run --release'
name: evolve
runtime:
  kind: rust
description: Agent rollout lifecycle — generate, register, fork, lineage, leaves for evolved functions

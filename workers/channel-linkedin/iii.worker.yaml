iii: v1
scripts:
  install: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo build --release'
  start: '[ -f "$HOME/.cargo/env" ] && . "$HOME/.cargo/env"; cargo run --release'
name: channel-linkedin
runtime:
  kind: rust
description: "LinkedIn channel adapter — handle messaging webhook + reply via LinkedIn REST API"

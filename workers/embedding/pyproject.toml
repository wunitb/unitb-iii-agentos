[project]
name = "agentos-embedding"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "iii-sdk==0.22.1",
    "sentence-transformers>=2.2.0",
    "torch>=2.0.0",
    "numpy>=1.24.0",
]

[project.optional-dependencies]
test = ["pytest>=8.0.0"]

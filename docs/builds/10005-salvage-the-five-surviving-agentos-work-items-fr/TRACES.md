# Build 10005 Traceability

This matrix maps every acceptance identifier to recovered behavior and durable
repository evidence. It records static evidence; fresh command results belong
in the delivery report.

| ISC identifier | Recovered behavior | Repository evidence |
|---|---|---|
| ISC-000 | Use canonical iii 0.22.1 worker names and prove the standalone queue provider survives a real boot. | `config.yaml`, `config/{queue,state,cron}.yaml`, `tests/config.test.ts`, and `tests/config_boot.test.ts` reject deprecated aliases and verify `engine::queue::enqueue` from `queue-engine`. |
| ISC-001 | Namespace all AgentOS router registrations and source call sites below `agentos::llm::*`, with collision-error behavior left as an upstream-only proposal for the principal. | `workers/llm-router/src/main.rs`, consumers under `workers/` and `crates/tui/`, `tests/llm_namespace.test.ts`, and `docs/decisions/2026-08-22-salvage-batch.md`. |
| ISC-002 | Keep Sessions and Security provider-backed while degrading Dashboard, Agents, Skills, Logs, Audit Events, and Settings to explicit no-provider states. | `workers/memory/src/main.rs`, `workers/security/src/main.rs`, `crates/tui/src/main.rs`, `tests/tui_surfaces.test.ts`, and the per-screen decision in `docs/decisions/2026-08-22-salvage-batch.md`. |
| ISC-003 | Provide ordered `agentos up` and `--no-tui` orchestration, read-only `agentos doctor`, documented config precedence, and the new quickstart. | `crates/cli/src/bootstrap.rs`, `crates/cli/src/main.rs`, `crates/cli/tests/up.rs`, `crates/cli/tests/portability.rs`, `README.md`, and `tests/quickstart.test.ts` cover mockable spawning, readiness decisions, failure diagnosis, and `AGENTOS_HOME` checkout discovery. |
| ISC-004 | Canonicalize expected runtime directories before comparing them with a child process's `$PWD`. | `crates/cli/tests/portability.rs` canonicalizes the affected expected paths, including the macOS `/var` versus `/private/var` alias. |
| ISC-005 | Keep a reproducible root Bun lockfile generated by frozen installation. | `bun.lock` and `tests/bun_lockfile.test.ts` enforce a tracked, non-empty lockfile with root direct-dependency coverage; repeated frozen installs, TypeScript checking, and the Bun suite are delivery checks. |

## Required verification

Run `bun install --frozen-lockfile` twice and confirm each run leaves the
checkout unchanged. Run `bunx tsc --noEmit` and `bun test`. Confirm every ISC
identifier occurs in this file, and compare the heading lists of each build
10000 and build 10005 artifact after normalizing the build number in its title.
